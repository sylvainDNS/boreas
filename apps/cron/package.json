{
  "name": "@boreas/cron",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc --persist-to ../../.wrangler/state --port 8788 --inspector-port 0 --test-scheduled",
    "typecheck": "tsc --noEmit",
    "build": "wrangler deploy --dry-run --config wrangler.jsonc"
  },
  "dependencies": {
    "@boreas/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260527.1",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  }
}
